{
  "name": "clinical-form-tool",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "pdf-lib": "^1.17.1"
  },
  "scripts": {
    "dev": "concurrently -k -n web,api \"vite\" \"node --watch --env-file-if-exists=.env server/index.js\"",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env server/index.js",
    "preview": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.0",
    "concurrently": "^10.0.5",
    "vite": "^8.2.2"
  }
}
