# Create a local .env file and never commit it.
ANTHROPIC_API_KEY=sk-ant-replace-me

# Optional. The server defaults to the current Sonnet model.
ANTHROPIC_MODEL=claude-sonnet-5

# Optional. Hosting platforms generally set PORT automatically.
PORT=8787
HOST=0.0.0.0
